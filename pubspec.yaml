name: conversor_moeda
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  http: ^1.2.1

  fl_chart: ^0.69.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
